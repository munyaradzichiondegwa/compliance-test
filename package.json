{
  "name": "zacc-institutional-compliance-portal",
  "version": "1.0.0",
  "private": true,
  "description": "ZACC Institutional Compliance Portal — full-stack monorepo (server + client)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "dev": "concurrently -n SERVER,CLIENT -c blue,magenta \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "setup": "npm run install:all && npm run seed && npm run build",
    "demo": "npm run setup && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
